= form_with url: resources[:form_url], local: false, class: resources[:form_class], html: resources[:form_html] do |f|
  = f.label :body
  = f.text_area :body, class: "form-control"
  = f.submit 'Comment', class: "submit-comment"